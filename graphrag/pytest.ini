[pytest]
# Tests import the service modules by their top-level names (`services`,
# `main`, `config`). Bare `pytest` does not put the working directory on
# sys.path - only `python -m pytest` does - so CI failed to collect while
# local runs worked. Pinning it here makes both behave the same.
pythonpath = .
asyncio_mode = auto
